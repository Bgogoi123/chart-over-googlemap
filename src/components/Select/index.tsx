function Select() {
  return (
    <div style={{ border: "2px solid red", padding: "0.5em 0 0.5em 0" }}>
      <select>
        <option selected> Select a Disease</option>
      </select>
    </div>
  );
}

export default Select;
